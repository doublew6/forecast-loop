# Bundled example log

This file records changes to the public synthetic examples only. It must never
contain an operator's research history. Operator Wiki maintenance belongs in
the local Wiki root's own append-only `log.md`.

## [2026-07-30] reset | demo-only public examples

- Replaced the published research catalog with three synthetic examples.
- Moved the runtime default to the Git-ignored `data/wiki/` tree.
- Added an explicit rule that Live runs never fall back to bundled examples.
